# this file signals to python that the `routers` directory should be treated as a package
# allows for importing modules e.g. items.py and users.py (a file is regarded as a module over here) into other parts of the app (backend)

# it can also contain initialization code that is run when the package is imported